from setuptools import setup, find_packages

install_requires = []

setup(
    name='homework_parser',
    version='0.0.1',
    description='I dont know what im doing...',
    author='firemark',
    install_requires=install_requires,
    packages=find_packages()
)
